pragma ever-solidity >= 0.62.0;

interface ProposalStates {
    enum ProposalState {
        Pending,
        Active,
        Canceled,
        Failed,
        Succeeded,
        Expired,
        Queued,
        Executed
    }
}
