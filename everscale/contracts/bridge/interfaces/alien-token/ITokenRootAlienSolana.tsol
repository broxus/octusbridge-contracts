pragma ever-solidity >= 0.62.0;


interface ITokenRootAlienSolana {
    function meta() external responsible returns (
        uint256 base_token,
        string name,
        string symbol,
        uint8 decimals
    );
}
