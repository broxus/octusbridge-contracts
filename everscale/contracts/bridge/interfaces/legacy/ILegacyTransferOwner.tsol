pragma ever-solidity >= 0.62.0;
pragma AbiHeader expire;

interface ILegacyTransferOwner {
    function transferOwner(
        uint256 external_owner_pubkey_,
        address internal_owner_address_
    ) external;
}
