const { ContractWrapper, requireContract } = require('./contract-wrapper');
const { TonWrapper } = require('./ton-wrapper');

module.exports = {
  ContractWrapper,
  TonWrapper,
  requireContract,
};
